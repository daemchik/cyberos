# Скопируйте в `.env.development` (или `.env`) в папке `cyberos/`, чтобы Vite подхватил переменные.
# Куда проксируется /api. 502 в браузере = сюда не удаётся подключиться (Django не слушает порт и т.п.).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
